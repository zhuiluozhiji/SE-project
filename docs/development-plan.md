# 开发计划

更新时间：2026-04-28  
适用阶段：基于当前已搭建工程骨架，进入第一阶段 MVP 业务开发。

## 1. 当前开发目标

当前项目已经完成前后端骨架、接口占位、数据库 SQL、爬虫骨架和基础测试目录。下一阶段不再重新搭架构，重点是在现有框架上跑通第一阶段主流程：

```text
登录/注册 -> 活动浏览与筛选 -> 活动详情 -> 课表导入 -> 冲突检测 -> 加入日程 -> 日历展示 -> ICS 导出 -> 后台维护活动 -> 爬虫补充活动数据
```

第一阶段以“可运行、可联调、可演示”为优先级。OCR、复杂推荐、复杂统计和社交类功能可以保留接口或做简化实现。

## 2. 已确定成员分工

| 成员 | 角色 | 主要负责内容 | 第一阶段重点交付 |
| --- | --- | --- | --- |
| zzy | 前端负责人 | 页面与交互开发 | 活动列表、活动详情、日历、课表导入、后台页面联调 |
| gsj | 爬虫与数据库负责人 | 数据来源与数据库设计 | 表结构、种子数据、活动样例数据、计院爬虫与入库 |
| mxy | 后端负责人 1 | 后端基础与通用接口 | 数据库连接、ORM 基础、登录注册、用户信息、活动基础接口 |
| lyy | 后端负责人 2 | 日历与课表业务 | 课表导入、截图识别接口预留、冲突检测、日程、ICS 导出 |
| pxl | 后端负责人 3 | 推荐与筛选排序 | 活动搜索、标签/类别/校区筛选、排序、推荐规则、后台辅助统计 |
| hh | 测试与文书负责人 | 测试管理与项目文档 | 测试用例、接口测试、Bug 记录、进度记录、用户手册和报告材料 |

## 3. 启动顺序与交接关系

### 3.1 第一批先开始

gsj、mxy、hh 可以最先开始。

| 成员 | 先做事项 | 交接给谁 | 交接物 |
| --- | --- | --- | --- |
| gsj | 确认 `database/schema.sql` 字段是否满足第一阶段，补充 `seed.sql` 活动样例 | mxy、lyy、pxl、hh | 数据表说明、测试账号、活动样例、字段变更记录 |
| mxy | 接入 MySQL、补全基础 ORM、实现登录和活动基础查询 | zzy、pxl、hh | `/auth/login`、`/users/me`、`/activities`、`/activities/{id}` 可用接口 |
| hh | 建立本阶段测试清单和接口变更记录模板 | 全体 | 测试用例表、Bug 记录表、每日进度记录格式 |

### 3.2 第二批并行推进

zzy、pxl、lyy 在第一批字段和接口基本稳定后进入联调，但不需要完全等待后端做完，可以先基于现有 mock 和接口规范开发页面。

| 成员 | 启动条件 | 主要任务 | 交接给谁 |
| --- | --- | --- | --- |
| zzy | API 字段初稿明确 | 活动列表、详情、登录状态、日历和课表导入页面联调 | hh、后端负责人 |
| pxl | activity、activity_tag、user_interest 表字段稳定 | 搜索筛选、排序、推荐分计算、推荐接口 | zzy、hh |
| lyy | schedule_event、course_schedule 表字段稳定 | 课程导入、日程查询、冲突检测、加入日程、ICS 导出 | zzy、hh |

### 3.3 第三批集成验收

当活动、推荐、日程三条后端主线都有可用接口后，进入集成测试。

| 成员 | 集成阶段任务 |
| --- | --- |
| zzy | 串起用户主流程，处理前端错误提示、空状态、加载状态和页面截图 |
| gsj | 补充 20-50 条真实或半真实活动数据，修正爬虫字段清洗问题 |
| mxy | 修复认证、权限、活动接口联调问题 |
| lyy | 修复课程、冲突检测、日历、ICS 导出问题 |
| pxl | 修复筛选、排序、推荐结果不准确问题 |
| hh | 执行主流程测试，维护 Bug 清单、测试报告和用户手册 |

## 4. 阶段排期建议

### 第 1 阶段：数据和基础接口优先

建议周期：1-2 天。

| 成员 | 任务 |
| --- | --- |
| gsj | 稳定数据库表结构，补充种子数据，整理字段说明 |
| mxy | 完成数据库连接、ORM 基础、用户登录、用户信息、活动列表和详情接口 |
| hh | 根据现有接口建立测试用例骨架，记录字段和接口变更 |
| zzy | 基于现有 mock 完成活动列表、详情、登录页的可联调结构 |
| pxl | 确认筛选和推荐所需字段，给出推荐分第一版规则 |
| lyy | 确认日程和课程表字段，设计冲突检测输入输出 |

阶段完成标志：

```text
前端能调用真实活动列表接口；
后端能从 MySQL 返回活动数据；
测试同学能用 Swagger 或接口测试工具跑通健康检查、登录、活动列表。
```

### 第 2 阶段：主流程联调

建议周期：3-5 天。

| 成员 | 任务 |
| --- | --- |
| zzy | 对接登录、活动列表、活动详情、推荐活动、日历事件接口 |
| gsj | 完成第一版爬虫数据解析，准备活动样例数据入库 |
| mxy | 完成注册、JWT 鉴权依赖、后台活动新增/编辑/下架基础接口 |
| lyy | 完成课程录入、CSV/Excel 导入、日程查询、冲突检测、加入日程 |
| pxl | 完成关键词搜索、类别/标签/校区筛选、热度/时间/推荐分排序 |
| hh | 开始执行接口测试和前后端联调测试，记录 Bug 和复现步骤 |

阶段完成标志：

```text
用户可以登录；
可以浏览和筛选活动；
可以查看活动详情；
可以导入或录入课程；
可以检测活动与课程冲突；
可以把活动加入日程并在日历中看到。
```

### 第 3 阶段：演示闭环补齐

建议周期：2-3 天。

| 成员 | 任务 |
| --- | --- |
| zzy | 完成页面交互细节、状态标签、错误提示和演示截图 |
| gsj | 修正爬虫清洗和去重，保证演示数据质量 |
| mxy | 完善错误响应、权限判断、管理员接口稳定性 |
| lyy | 完成 ICS 导出，OCR 接口可根据时间做预留或简化演示 |
| pxl | 调整推荐理由和推荐排序，补充后台统计接口 |
| hh | 完成第一轮系统测试、测试报告初稿、用户手册初稿 |

阶段完成标志：

```text
答辩演示主流程可以连续跑通；
测试报告有执行记录；
用户手册能指导普通用户和管理员完成核心操作；
Bug 清单中没有阻断演示的问题。
```

### 第 4 阶段：收尾与答辩材料

建议周期：1-2 天。

| 成员 | 任务 |
| --- | --- |
| 全体 | 修复高优先级 Bug，统一演示环境，确认代码合并状态 |
| zzy | 准备前端展示截图和演示流程说明 |
| gsj | 准备数据库 ER 图、活动数据来源说明、爬虫运行截图 |
| mxy、lyy、pxl | 准备后端接口、核心逻辑和 Swagger 截图 |
| hh | 汇总测试报告、用户手册、进度文档和答辩材料 |



## 6. 当前最优先任务清单

1. gsj：确认并冻结第一阶段数据库字段，补充活动样例数据。
2. mxy：让活动列表和登录接口从 mock 进入真实数据库联调。
3. zzy：准备活动列表、详情、日历页面的真实接口接入。
4. lyy：设计并实现冲突检测、加入日程的最小闭环。
5. pxl：实现活动搜索、筛选、排序和规则推荐。
6. hh：建立测试用例、Bug 记录和接口联调问题清单。
