# 前端开发进度

更新时间：2026-05-23  
负责人：zzy  
当前阶段：UI/UX 打磨 + 暗色模式完成，主流程可联调

## 1. 一句话总结

前端已从「骨架占位 + mock 数据」阶段整体升级为「真实 API 对接 + 完整交互」阶段。所有 8 个页面均已接入后端接口，登录鉴权、路由守卫、错误处理、加载/空/错误状态覆盖完毕，主流程可连续跑通。

## 2. 前端技术栈

```text
Vue 3.5+        (Composition API)
Vite 6          (构建工具 + HMR)
Element Plus 2.9 (UI 组件库)
Vue Router 4.5  (路由 + 懒加载)
Pinia 2.3       (状态管理)
Axios 1.7       (HTTP 请求)
```

## 3. 前端文件结构

```text
frontend/src/
├── main.js                      # 应用入口，挂载 Pinia/Router/ElementPlus
├── App.vue                      # 根布局：侧边栏导航 + 顶部栏 + 路由出口
├── router/index.js              # 8 条路由 + beforeEach 鉴权守卫
├── store/auth.js                # 登录状态管理（token/用户持久化）
├── styles/main.css              # 全局样式 + CSS 变量 + 设计系统
├── api/
│   ├── http.js                  # Axios 实例 + 请求/响应拦截器
│   ├── auth.js                  # 登录、获取当前用户
│   ├── activities.js            # 活动列表、活动详情
│   ├── recommendations.js        # 推荐活动
│   ├── schedules.js             # 日程、冲突检测、加入日程、ICS 导出
│   ├── courses.js               # 课程新增、课表导入、OCR
│   └── admin.js                 # 后台活动管理、爬虫触发
├── views/
│   ├── HomeView.vue             # 首页推荐
│   ├── LoginView.vue            # 登录/注册
│   ├── ActivityListView.vue     # 活动列表（筛选/分页）
│   ├── ActivityDetailView.vue   # 活动详情（冲突/加入日程）
│   ├── CalendarView.vue         # 个人日历（周视图/ICS 导出）
│   ├── CourseImportView.vue     # 课表导入（文件上传/手动录入）
│   ├── ProfileView.vue          # 个人中心
│   └── AdminView.vue            # 后台管理
├── components/
│   ├── ActivityCard.vue         # 可复用活动卡片组件
│   ├── StatusTag.vue            # 状态标签组件
│   └── EmptyState.vue           # 空状态占位组件
├── composables/
│   ├── useDarkMode.js           # 暗色模式状态管理 + localStorage 持久化
│   └── useBackgroundTheme.js    # 背景设置状态管理 + CSS 变量写入 + localStorage 持久化
└── utils/
    └── constants.js             # 活动状态/事件颜色常量映射
```

## 4. 各模块完成情况

### 4.1 基础设施层 — 已完成

| 项目 | 文件 | 完成内容 |
|------|------|----------|
| HTTP 拦截器 | [http.js](../frontend/src/api/http.js) | 请求拦截自动附加 JWT Bearer token；响应拦截统一解包 `{code, message, data}`、非 0 业务码自动 ElMessage 报错、401 清除 token 跳转登录页、网络超时/异常统一提示 |
| 认证状态管理 | [auth.js](../frontend/src/store/auth.js) | `login()` 异步登录并持久化 token+user 到 localStorage；`fetchUser()` 获取当前用户；`logout()` 清除会话；刷新页面后从 localStorage 恢复状态 |
| 路由鉴权 | [index.js](../frontend/src/router/index.js) | `beforeEach` 守卫检查 token，个人日历/课表导入/个人中心/后台管理 4 个页面标记 `requiresAuth`，未登录重定向 `/login`；已登录访问 `/login` 自动跳转首页 |
| 全局布局 | [App.vue](../frontend/src/App.vue) | 侧边栏 6 项导航 + sticky 全高布局；用户信息/登录按钮移至侧边栏底部；顶部栏仅含页面标题 + 搜索框；搜索框回车跳转活动列表并携带 keyword 参数；暗色模式切换按钮 |
| 全局样式 | [main.css](../frontend/src/styles/main.css) | CSS 自定义属性（品牌色、暖色调纸质感）、`[data-theme="dark"]` 暗色模式变量覆盖、卡片/标签/hero/动画等设计系统 |

### 4.2 登录注册页 — 已完成

| 项目 | 状态 |
|------|------|
| 文件 | [LoginView.vue](../frontend/src/views/LoginView.vue) |
| 表单绑定 | 用户名、密码双向绑定 + Element Plus 表单校验（必填、最小长度） |
| 登录 | 调用 `auth.login()` → 后端 `POST /auth/login` → 成功跳转首页 → 失败展示错误消息 |
| 注册 | 表单切换 + 确认密码校验，注册功能提示后续开放，保留完整 UI |
| 状态处理 | loading 状态禁用按钮防重复提交；错误由拦截器统一提示 |

### 4.3 活动列表页 — 已完成

| 项目 | 状态 |
|------|------|
| 文件 | [ActivityListView.vue](../frontend/src/views/ActivityListView.vue) |
| 筛选条件 | 关键词（输入框）、校区（下拉选择：紫金港/玉泉/西溪）、类别（下拉选择：学术讲座/研讨会/工作坊）、排序（最新发布/最热门/推荐优先） |
| 分页 | Element Plus 分页器，切换页码自动重新请求，显示总数 |
| 数据渲染 | 卡片网格展示，每张卡片包含状态标签、标题、描述截断、标签组、校区地点 |
| 跨页联动 | 从首页搜索框或 URL query 携带 keyword 进入时自动回填筛选条件并查询 |
| 状态处理 | loading 态（v-loading）；空数据态（提示调整筛选条件）；错误态（显示错误信息 + 重试按钮） |

### 4.4 活动详情页 — 已完成

| 项目 | 状态 |
|------|------|
| 文件 | [ActivityDetailView.vue](../frontend/src/views/ActivityDetailView.vue) |
| 详情渲染 | 根据路由 `params.id` 调用 `GET /activities/{id}`，展示标题、主讲人、组织单位、时间、地点、类别、标签、简介 |
| 冲突检测 | 页面加载时自动静默检测冲突，不弹窗；仅当点击「加入日程」时若存在冲突则弹出冲突明细弹窗 |
| 加入日程 | 「加入日程」按钮调用 `POST /schedules/add-activity`；若有冲突先弹窗确认再提交，成功后 ElMessage 提示 |
| 弹窗修复 | 所有 `el-dialog` 移出 `.fade-in` 容器，添加 `append-to-body align-center` 属性，避免 CSS transform 引起遮罩只覆盖局部 |
| 状态处理 | loading / 错误 / 数据不存在三种状态各自有 UI；未登录用户点击加入日程会触发路由守卫重定向登录 |

### 4.5 首页推荐 — 已完成

| 项目 | 状态 |
|------|------|
| 文件 | [HomeView.vue](../frontend/src/views/HomeView.vue) |
| 推荐列表 | 调用 `GET /recommendations/activities`，渲染推荐活动卡片（含推荐理由、时间、地点） |
| 统计数字 | Hero 区域对接活动总数统计；非关键数据静默失败不影响页面 |
| 快捷跳转 | 「立即探索」→ `/activities`；「导入课表」→ `/courses/import` |
| 加载态 | 骨架屏 shimmer 动画（3 个占位卡片） |
| 错误态 | 显示错误信息 + 重试按钮 |

### 4.6 个人日历页 — 已完成

| 项目 | 状态 |
|------|------|
| 文件 | [CalendarView.vue](../frontend/src/views/CalendarView.vue) |
| 周视图 | 7 天网格布局，统一格式无今日高亮；根据后端返回的 `start_time` 将事件分配到对应日期的对应时段槽位 |
| 事件渲染 | 每种事件类型有独立颜色（课程蓝色/活动绿色/推荐橙色/冲突红色/考试紫色）；显示标题和时间，暗色模式自动适配 |
| 周切换 | 「上一周」「下一周」按钮，切换后自动重新请求该周的日程数据 |
| 侧边栏 | 「即将开始」列表取最近 5 个未过期事件；冲突面板统计本周冲突数量；侧边栏 sticky 全高显示 |
| ICS 导出 | 「导出 ICS」按钮通过 fetch 调用 `GET /schedules/export-ics`，处理后端返回的 blob 文件触发浏览器下载 |
| 弹窗修复 | 冲突弹窗移出 section 容器，添加 `append-to-body align-center`，遮罩覆盖全页面 |
| 状态处理 | loading / 错误 / 无数据三种状态覆盖 |

### 4.7 课表导入页 — 已完成

| 项目 | 状态 |
|------|------|
| 文件 | [CourseImportView.vue](../frontend/src/views/CourseImportView.vue) |
| 文件上传 | Element Plus Upload 拖拽组件，限制 CSV/Excel 格式，调用 `POST /courses/import`（FormData） |
| 解析结果展示 | 解析成功后以表格展示课程名、教师、星期、节次、教室，用户可「确认导入」逐条调用 `POST /courses` 保存，或「清除重选」 |
| 手动录入 | 右侧表单支持手动添加单门课程（课程名、教师、星期选择器、节次数字输入、教室） |
| 错误处理 | 解析失败显示错误面板 + 重试按钮；手动录入时课程名为空给出警告 |

### 4.8 个人中心页 — 已完成

| 项目 | 状态 |
|------|------|
| 文件 | [ProfileView.vue](../frontend/src/views/ProfileView.vue) |
| 用户信息 | 从 auth store 读取当前用户，展示用户名、学院、专业 |
| 兴趣标签 | 从用户数据中读取 `tags`/`interests` 字段渲染标签组 |
| 学术轨迹 | 时间线组件展示用户历史活动记录 |
| 外观设置 | 支持“整个网页背景 / 个人日历背景”两种范围的 `默认 / 纯色 / 渐变` 配置，含实时预览、保存设置、恢复默认 |
| 数据刷新 | 挂载时调用 `auth.fetchUser()` 获取最新用户数据 |

### 4.9 后台管理页 — 已完成

| 项目 | 状态 |
|------|------|
| 文件 | [AdminView.vue](../frontend/src/views/AdminView.vue) |
| 活动管理表格 | 加载所有活动数据到表格，支持按标题本地搜索过滤 |
| 新增/编辑活动 | 弹窗表单含标题、主讲人、组织单位、校区、类别、地点、开始/结束时间、简介，调用 `POST/PUT /admin/activities` |
| 下架活动 | Popconfirm 确认后调用 `DELETE /admin/activities/{id}`，成功后刷新列表 |
| 触发爬虫 | 调用 `POST /admin/crawler/run`，按钮 loading 态 |
| 爬虫记录 | 弹窗查看爬虫运行记录（预留接口对接） |

## 5. 组件与 Composables

### 5.1 组件

| 组件 | 文件 | 功能 |
|------|------|------|
| StatusTag | [StatusTag.vue](../frontend/src/components/StatusTag.vue) | 根据状态字段（open/full/closed/offline/conflict）映射 Element Plus Tag 的颜色类型（success/warning/info/danger）和文案 |
| EmptyState | [EmptyState.vue](../frontend/src/components/EmptyState.vue) | 通用空状态/占位组件，支持自定义标题和描述 |
| ActivityCard | [ActivityCard.vue](../frontend/src/components/ActivityCard.vue) | 可复用的活动卡片（ActivityListView / HomeView 共用），含 hover 动效（箭头平移）、状态标签、标签组、时间格式化、描述截断；均已使用 CSS 变量，暗色模式自动适配 |

### 5.2 Composables

| Composable | 文件 | 功能 |
|------------|------|------|
| useDarkMode | [useDarkMode.js](../frontend/src/composables/useDarkMode.js) | 暗色模式状态管理：`isDark` ref（响应式）、`init()` 读取 localStorage 或系统偏好、`toggle()` 切换并持久化；同时设置 `data-theme` 属性（自定义变量）和 `dark` class（Element Plus 暗色模式） |
| useBackgroundTheme | [useBackgroundTheme.js](../frontend/src/composables/useBackgroundTheme.js) | 自定义背景状态管理：维护全站 / 日历两组背景配置，负责模式归一化、CSS 变量写入、`localStorage` 持久化、恢复默认 |

## 6. API 对接覆盖

所有 7 个 API 模块的函数签名均已在页面中调用：

| API 模块 | 函数 | 调用页面 |
|----------|------|----------|
| auth.js | `login()` | LoginView |
| auth.js | `getCurrentUser()` | ProfileView（通过 auth.fetchUser） |
| activities.js | `getActivities()` | ActivityListView, HomeView, AdminView |
| activities.js | `getActivityDetail()` | ActivityDetailView |
| recommendations.js | `getRecommendedActivities()` | HomeView |
| schedules.js | `getSchedules()` | CalendarView |
| schedules.js | `checkConflict()` | ActivityDetailView |
| schedules.js | `addActivityToSchedule()` | ActivityDetailView |
| schedules.js | `exportIcs()` | CalendarView |
| courses.js | `importCourses()` | CourseImportView |
| courses.js | `createCourse()` | CourseImportView |
| admin.js | `createActivity()` | AdminView |
| admin.js | `updateActivity()` | AdminView |
| admin.js | `offlineActivity()` | AdminView |
| admin.js | `runCrawler()` | AdminView |

## 7. 交互状态覆盖

每个需要异步数据的页面均覆盖了四种状态：

| 状态 | 触发条件 | UI 表现 |
|------|----------|---------|
| 加载中 | API 请求进行中 | 骨架屏（首页）/ v-loading（列表、详情、日历）/ 按钮 loading 禁用（提交类操作） |
| 成功 | API 返回正常数据 | 正常渲染页面内容 |
| 空数据 | 接口返回空数组 | 提示文字（如"暂无活动，试试调整筛选条件"） |
| 错误 | 网络异常/超时/业务错误码 | ElMessage 错误提示（http 拦截器统一处理）+ 页面错误面板 + 重试按钮 |

## 8. 用户主流程连通性

以下主流程从前端侧已全部走通：

```text
[登录]  LoginView → 输入用户名密码 → auth.login() → 存 token → 跳转首页
   ↓
[浏览]  HomeView → 推荐活动卡片 → 点击查看详情
   ↓                    ↓
[筛选]  ActivityListView → 关键词/校区/类别/排序 → 分页翻页
   ↓
[详情]  ActivityDetailView → 查看信息 → 检测冲突 → 弹窗展示冲突
   ↓                                              ↓
[日程]  点击「加入日程」→ addActivityToSchedule() → "已加入日程"
   ↓
[日历]  CalendarView → 周视图展示课程+活动 → 不同颜色区分 → 导出 ICS
   ↓
[课表]  CourseImportView → CSV/Excel 上传解析 → 手动录入 → 确认导入
   ↓
[管理]  AdminView → 新增/编辑活动 → 下架确认 → 触发爬虫
   ↓
[个人]  ProfileView → 查看用户信息 → 兴趣标签 → 学术轨迹
```

## 9. 当前未实现内容

以下功能保留 UI 占位，依赖后端接口就绪后对接：

```text
- 注册功能（登录页有 UI 切换，但后端注册接口尚未就绪，注册时提示"后续版本开放"）
- OCR 截图导入（课表导入页保留入口，接口 `POST /courses/ocr` 已封装，UI 后续扩展）
- 爬虫运行记录弹窗（弹窗框架已写好，等待后端爬虫记录接口返回真实数据后填充）
- 首页统计数据（总活动数已对接，匹配数/已加入数依赖后端统计接口）
- 移动端深度适配（已做基础响应式断点 960px，未做移动端窄屏侧边栏折叠菜单）
```

## 10. 上游依赖

前端代码本身已完成，但页面实际数据取决于后端接口返回。需要后端同学确保以下接口返回格式一致：

| 后端负责人 | 需对齐的接口 | 前端期望返回格式 |
|------------|-------------|-----------------|
| mxy | `POST /auth/login` | `{ code: 0, data: { token: "...", user: { username, college, ... } } }` |
| mxy | `GET /users/me` | `{ code: 0, data: { username, college, major, tags: [...], ... } }` |
| mxy | `GET/POST/PUT/DELETE /admin/activities/*` | 标准 CRUD，活动对象含 id, title, status 等 |
| pxl | `GET /activities` | `{ code: 0, data: { items: [...], total: N } }`，items 含 id, title, campus, location, start_time, status, tags, description |
| pxl | `GET /recommendations/activities` | `{ code: 0, data: { items: [...], total: N } }`，item 含 id, title, reason, start_time, campus, location |
| lyy | `GET /schedules` | `{ code: 0, data: { events: [...] } }`，event 含 title, type/color_type, start_time, location |
| lyy | `POST /schedules/check-conflict` | `{ code: 0, data: { conflicts: [...] } }` |
| lyy | `POST /schedules/add-activity` | `{ code: 0, message: "success" }` |
| lyy | `GET /schedules/export-ics` | 返回 ICS 文件流（blob） |
| lyy | `POST /courses/import` | `{ code: 0, data: { courses: [...] } }`，course 含 course_name, teacher, weekday, start_section, end_section, location |

## 11. 适配说明

- **响应式**：所有页面在 960px 断点以下切换为单列布局，侧边栏保持 232px 固定宽度
- **容器化**：Vite 配置了 `watch.usePolling` 通过环境变量 `CHOKIDAR_USEPOLLING` 控制，Docker 环境下开启以支持文件监听
- **API 代理**：Vite dev server 将 `/api` 请求代理到 `VITE_DEV_PROXY_TARGET`（Docker 环境下为 `http://backend:8000`，本地开发为 `http://localhost:8000`）

## 12. UI/UX 打磨（2026-05-23）

本轮针对交互细节、布局一致性、弹窗行为和视觉密度进行了系统优化：

| 改动项 | 影响范围 | 说明 |
|--------|----------|------|
| 用户信息下沉侧边栏 | App.vue | 将用户头像/用户名/学院从顶部栏移至侧边栏底部，登录/退出按钮同行排列；侧边栏 `height: 100vh; position: sticky` 确保所有页面侧边栏全高显示 |
| 页面留白压缩 | HomeView, ProfileView, AdminView, ActivityDetailView | Hero 区域 padding 40px→28px、page-panel 28px→20px、profile-hero 28px→20px，增大主体内容区域 |
| 冲突检测流程重构 | ActivityDetailView | 移除「检测冲突」按钮；页面加载时自动静默调用冲突检测 API；仅在点击「加入日程」且存在冲突时才弹出冲突明细弹窗 |
| 弹窗遮罩修复 | ActivityDetailView, AdminView, CalendarView | 所有 `el-dialog` 移出 `.fade-in` 动画容器，添加 `append-to-body align-center` 属性，修复 CSS `transform` 导致遮罩只覆盖局部页面的问题 |
| 日历今日高亮移除 | CalendarView | 移除「今日」高亮样式和 `isToday` class 绑定，所有日期统一格式显示 |
| 日历暗色适配 | CalendarView | 课程/活动/考试 dot、timeline 事件背景、冲突/考试横幅均在暗色模式下有对应配色 |
| 硬编码颜色消除 | 全局 | `#ffffff`/`#fff` 替换为 `var(--bg-surface)`，chip/danger/warning 边框等硬编码色添加暗色模式覆盖 |

## 13. 暗色模式

基于 `[data-theme="dark"]` 属性选择器 + Element Plus `dark` class 双通道实现：

- **CSS 变量覆盖**：`main.css` 中定义暗色模式下全套变量（背景系 `#1a1d21`~`#2a2e33`，文字系 `#e8e6e3`~`#5a554f`，语义色整体提亮降低饱和度，阴影改用纯黑透明度）
- **Element Plus 集成**：`useDarkMode` composable 同步添加 `dark` class 到 `<html>`，激活 Element Plus 内置暗色组件样式
- **状态持久化**：主题选择存入 `localStorage`（key: `se-theme`），刷新不丢失；首次访问跟随系统 `prefers-color-scheme` 偏好
- **切换按钮**：侧边栏底部常驻，暗色模式显示 ☀️ 图标（切回亮色），亮色模式显示 🌙 图标（切到暗色）
- **页面适配**：CalendarView（事件颜色）、CourseImportView（提示边框）、ActivityDetailView（演讲者头像、冲突 banner）均已添加暗色模式覆盖样式

## 14. 背景自定义第一版（2026-06-02）

目标：在不引入后端依赖的前提下，为用户提供“整个网页背景 / 个人日历背景”的本地自定义能力，支持实时预览、保存和恢复默认。

### 14.1 功能边界

- [x] 仅做前端本地版，不接后端，不做账号同步
- [x] 支持两个作用范围：`整个网页背景`、`个人日历背景`
- [x] 支持三种模式：`默认`、`纯色`、`渐变`
- [x] 支持遮罩强度调节，保证内容可读性
- [x] 支持实时预览
- [x] 支持 `localStorage` 持久化
- [x] 支持恢复默认
- [ ] 暂不支持图片上传背景

### 14.2 代码改动清单

| 状态 | 文件 | 改动内容 |
|------|------|----------|
| [x] | `frontend/src/composables/useBackgroundTheme.js` | 新增背景设置 composable，统一管理默认配置、配置归一化、CSS 变量写入、`localStorage` 持久化、恢复默认 |
| [x] | `frontend/src/styles/main.css` | 新增背景相关 CSS 变量；为 `.app-shell` 增加全站背景层和遮罩层；增加暗色模式下背景遮罩基础变量 |
| [x] | `frontend/src/App.vue` | 在应用启动时初始化背景设置，使页面刷新后自定义背景立即恢复 |
| [x] | `frontend/src/views/CalendarView.vue` | 为个人日历页增加独立背景层，读取并展示日历背景设置 |
| [x] | `frontend/src/views/ProfileView.vue` | 新增“外观设置”面板，提供作用范围切换、模式切换、颜色选择、渐变角度、遮罩强度、保存与恢复默认 |
| [x] | `progress/前端进度.md` | 记录第一版设计边界、实现清单与落地文件 |

### 14.3 交互说明

- 用户在“个人中心”进入“外观设置”
- 选择作用范围后，可分别设置网页背景或个人日历背景
- 切换 `默认 / 纯色 / 渐变` 时页面实时预览
- 点击“保存设置”后写入浏览器本地存储，刷新页面后仍保留
- 点击“恢复默认”后仅重置当前作用范围的背景配置

### 14.4 验收标准

1. 刷新页面后，自定义背景仍能正确恢复
2. 亮色 / 暗色模式下文本可读性不受背景影响
3. 全站背景与个人日历背景可以分别设置、互不干扰
4. 恢复默认后，对应作用范围回到系统默认背景
5. 不修改后端接口、不引入新依赖

### 14.5 第二步扩展：卡片外观自定义（2026-06-02）

目标：在第一版“背景 DIY”基础上继续扩展，让用户可以分别为“整个网页 / 个人日历”设置内容卡片的颜色、透明度和文字颜色，仍然保持前端本地版、实时预览和持久化保存。

#### 14.5.1 新增能力

- [x] 支持两种作用范围下分别配置卡片样式：`整个网页`、`个人日历`
- [x] 支持卡片模式切换：`默认`、`自定义`
- [x] 支持卡片颜色调节
- [x] 支持卡片透明度调节
- [x] 支持主文字颜色调节
- [x] 支持辅助文字颜色调节
- [x] 支持预览面板同步展示卡片颜色与文字层级变化
- [x] 支持 `localStorage` 持久化和恢复默认

#### 14.5.2 代码落地清单

| 状态 | 文件 | 改动内容 |
|------|------|----------|
| [x] | `frontend/src/composables/useBackgroundTheme.js` | 扩展配置项，新增卡片模式/颜色/透明度/主副文字色；写入卡片相关 CSS 变量，并同步根节点作用范围模式标记 |
| [x] | `frontend/src/styles/main.css` | 为全局 `.page-panel` / `.card` / `.hero` 增加按作用范围生效的自定义卡片样式覆盖 |
| [x] | `frontend/src/views/CalendarView.vue` | 为个人日历头部、主时间轴卡片、侧边栏卡片增加独立卡片外观覆盖，保留课程事件块原有语义色 |
| [x] | `frontend/src/views/ProfileView.vue` | 外观设置面板新增卡片样式控制项，并扩展实时预览区域展示卡片颜色、透明度和文字色变化 |

## 15. 下一步计划

1. 等待后端接口返回真实数据后进行联调，验证字段名对齐
2. 根据后端实际返回结构调整前端数据解析逻辑
3. 补充移动端侧边栏折叠/汉堡菜单
4. 对接注册接口（后端就绪后）
5. 对接统计接口完善首页数字面板
6. 对接爬虫记录接口填充日志弹窗
